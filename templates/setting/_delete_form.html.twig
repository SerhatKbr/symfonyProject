<form method="post" action="{{ path('setting_delete', {'id': setting.id}) }}" onsubmit="return confirm('Silmek istediğinize emin misiniz?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ setting.id) }}">
    <button  class="btn btn-primary"><i class="icon-pencil icon-white"></i>Sil</button>
</form>
