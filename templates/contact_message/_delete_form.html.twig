<form method="post" action="{{ path('contact_message_delete', {'id': contact_message.id}) }}" onsubmit="return confirm('Silmek istediğinize emin misiniz?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_message.id) }}">
    <button  class="btn btn-primary"><i class="icon-pencil icon-white"></i>Sil</button>
</form>
